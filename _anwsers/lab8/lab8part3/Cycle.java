package lab8part3;

public abstract class Cycle extends Transport{
    public Cycle(float acceleration, float maxSpeed, float currentSpeed) {
        super(acceleration, maxSpeed, currentSpeed);
    }
}
