package lab8part3;

public abstract class RoadVehicle extends Transport {

    public RoadVehicle(float acceleration, float maxSpeed, float currentSpeed) {
        super(acceleration, maxSpeed, currentSpeed);
    }
}
